#ifndef BFG_FILTER_READS
#define BFG_FILTER_READS

/* Short description: 
 *  - Go through reads and insert kmers into bloom filters 
 * */
void FilterReads(int argc, char** argv);

#endif // BFG_FILTER_READS
